#!/usr/bin/make -f

DH_VERBOSE = 1

%:
	dh $@ --with systemd

override_dh_auto_install:
	dh_auto_install
	dh_systemd_enable --name=afs
	mkdir -p debian/kafs-client/etc/kafs
	cp conf/etc.conf debian/kafs-client/etc/kafs

